import tensorflow as tf
import numpy as np
from features.symmetry_function import feature_generator
from models.neural_network import run

"""
  sample code
  - goal: easy to generate

inputs = read_input?(...)
feature_generator(...) -> generate feature vector and file list for training
run(...)

with tf.Session() as sess:
    train_run(sess, ...) -> run the training process
"""
#with tf.Session() as sess:
    #model = 
    #model_nn.run()